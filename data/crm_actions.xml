<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Server Action to Sync Student Fields -->
    <record id="action_server_sync_student_fields" model="ir.actions.server">
        <field name="name">Sync Student Fields</field>
        <field name="model_id" ref="crm.model_crm_lead"/>
        <field name="binding_model_id" ref="crm.model_crm_lead"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">
if records:
    action = records.action_sync_student_fields()
        </field>
    </record>

</odoo>
